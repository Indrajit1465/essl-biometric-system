fastapi>=0.110.0
uvicorn[standard]>=0.29.0
sqlalchemy>=2.0.0
pydantic-settings>=2.0.0
python-multipart>=0.0.9

# MySQL (production)
aiomysql>=0.2.0
PyMySQL>=1.1.0

# SQLite (development fallback — optional)
aiosqlite>=0.20.0

# Pull mode — device communication
pyzk>=0.9

# Scheduling — device health monitoring
apscheduler>=3.10.0

# Rate limiting
slowapi>=0.1.9